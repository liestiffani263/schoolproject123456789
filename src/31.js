function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Example usage:
const randomNumber = getRandomInt(100);
console.log(`Random number between 0 and ${randomNumber}: ${randomNumber}`);
