function getRandomInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// Example usage: 
console.log(getRandomInt(1, 100)); // Output a random integer between 1 and 100 (inclusive)
